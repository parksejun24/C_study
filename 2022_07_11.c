#include<stdio.h>
#include<stdlib.h>

/*
int main(){
    int length=0;
    int maxmin[2] = {0,};
    scanf("%d ",&length);
    int *num;
    num = malloc(sizeof(int)*length);
    for(int i = 0;i<length;i++){
        scanf(" %d",&num[i]);
    }
    maxmin[1] = num[0];
    for(int i=0;i<length;i++){
        if(maxmin[0] <= num[i]){
            maxmin[0] = num[i];
        }
        if(maxmin[1] >= num[i]){
            maxmin[1] = num[i];
        }
    }
    printf("%d %d",maxmin[1], maxmin[0]);	
}
*/

int main(){
           
}
